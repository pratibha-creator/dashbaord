// main.js — Dashboard entry point
// CSV parsing and chart logic will be added here once the CSV file is shared.

document.addEventListener('DOMContentLoaded', () => {
  console.log('Dashboard ready.');
});
